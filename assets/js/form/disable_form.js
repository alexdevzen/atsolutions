document.getElementById('contact-form').addEventListener('submit', function (event) {
    // Deshabilita el botón de enviar para evitar múltiples envíos del formulario
    document.getElementById('enviar-btn').disabled = true;
});

